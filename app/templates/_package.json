{
  "name": "<%= _.dasherize(appname) %>",
  "version": "0.0.0",
  "description": "Your project description",
  "license": "MIT",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "start": "node ./bin/www.js"
  },
  "files": [
    "app"
  ],
  "keywords": [
    "yeoman-generator"
  ],
  "dependencies": {
    "express": "^4.12.3",
    "body-parser": "^1.12.2",
    "compression": "^1.4.3",
    "morgan": "^1.5.2",
    "cookie-parser": "^1.3.4",
    "serve-favicon": "^2.2.0",<% if (hasJquery) { %>
    "jquery": "^2.1.3",<% } %><% if (hasReact) { %>
    "classnames": "^1.2.0",
    "react": "^0.13.1",<% if (hasBootstrap) { %>
    "react-bootstrap": "^0.17.0",<% } %><% } %>
    "hbs": "^3.0.1"
  },
  "devDependencies": {
    "del": "^1.1.1",
    "event-stream": "^3.3.0",
    "run-sequence": "^1.0.2",
    "vinyl-source-stream": "^1.1.0",
    "require-dir": "^0.2.0",
    "browserify": "^9.0.3",
    "watchify": "^2.5.0",
    "envify": "^3.4.0",
    "gulp": "^3.8.11",
    "gulp-nodemon": "^2.0.2",
    "gulp-notify": "^2.2.0"<% if (cssLanguage === 'less') { %>,
    "gulp-less": "^3.0.2",<% } %>
    "merge-stream": "^0.1.7"<% if (hasReact) { %>,
    "react-tools": "^0.13.1",
    "reactify": "^1.1.0"<% } %>
  }
}